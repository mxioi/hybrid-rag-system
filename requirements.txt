# Core dependencies
requests>=2.31.0
chromadb>=0.4.0

# Embeddings - sentence-transformers (preferred over onnxruntime)
sentence-transformers>=2.2.0
torch>=2.0.0  # Use CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu

# Claude API (for hybrid fallback)
anthropic>=0.76.0

# Optional - for API server
fastapi>=0.109.0
uvicorn>=0.27.0

# Optional - for web search
ddgs>=9.10.0
beautifulsoup4>=4.12.0

# Optional - for monitoring
prometheus-client>=0.19.0

# Optional - for caching
redis>=5.0.1

# Utilities
pyyaml>=6.0
